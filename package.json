{
  "name": "facebook-cli",
  "module": "src/index.ts",
  "type": "module",
  "private": true,
  "bin": {
    "facebook-cli": "./src/index.ts"
  },
  "scripts": {
    "dev": "bun run src/index.ts",
    "build": "bun build src/index.ts --outdir dist --target node",
    "test": "bun test",
    "test:watch": "bun test --watch",
    "typecheck": "tsc --noEmit",
    "lint": "echo 'No linter configured'"
  },
  "devDependencies": {
    "@types/bun": "latest",
    "typescript": "^5"
  },
  "dependencies": {
    "@bunli/core": "^0.5.4",
    "playwright": "^1.58.2",
    "zod": "^4.3.6"
  }
}
